# Edmund's DotFiles

This installs my dotfiles.

## Installation

    $ git clone git@github.com:edmundsalvacion/dotfiles.git
    $ cd dotfiles
    $ git submodule init
    $ git submodule update
    $ rake install
